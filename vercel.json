{
  "buildCommand": "pnpm run typecheck && pnpm --filter @workspace/iic-app run build",
  "outputDirectory": "artifacts/iic-app/dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
