# To implement the requirements we need:
# 1. Update `UserCustomTheme` in `src/types.ts` to include per-tab theme fields.
# 2. Update `ThemeState` in `src/components/ThemeCustomizer.tsx` to match.
# 3. Add default per-tab colors to `DEFAULT_THEME` in `src/components/ThemeCustomizer.tsx`.
# 4. Add UI in `ThemeCustomizer.tsx` to let users customize the colors and wallpapers.
# 5. In `StudentDashboard.tsx`, logic to use those per-tab colors and wallpapers depending on active tab.
# 6. Global wallpaper and per-tab custom wallpapers implementation.
# 7. Check the UI exemptions.
