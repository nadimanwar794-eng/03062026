import React, { useState, useEffect, useRef } from 'react';
import { Home, MessageSquare, User as UserIcon, Search, Settings } from 'lucide-react'; // Example icons

export const AppNav = () => {
    return <div>hi</div>
}
